<!DOCTYPE html>
<html>
  <head>
    <title>Bookish</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>


  <ul>
    <li style="display:inline-block"><h1><%= link_to "A Novel Website", root_path, :id => "mainHeader" %></h1></li>
    <li style="display:inline-block"></li>
    <% unless user_signed_in? %>
    <li style="display:inline-block; float: right;"> <%= button_to "Log In or Sign Up", new_user_session_path, :method => :get, :id => "loginButton" %></li>
    <% else %>
      <li style="display:inline-block; float: right;"><%= button_to(
            "Log Out",
            destroy_user_session_path,
            method: :delete,
            :id => "loginButton"
          ) %>
      <li style="display:inline-block; float: right; margin-top: 20px;"> Currently logged in as <b><%= current_user.email %></b> </li>
  <% end %></li>

  </ul>
  <hr>
    <%= yield %>
  </body>
</html>
